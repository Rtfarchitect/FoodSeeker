from setuptools import setup, find_packages

setup(
    name="FoodSeeker",
    version="0.1.0",
    packages=find_packages(),  # Automatically include all Python packages in the directory
    install_requires=[],       # Add dependencies here if needed
)
